{
  "name": "revane",
  "version": "1.0.2",
  "description": "inversion of control framework",
  "main": "bin/src/revane/Revane.js",
  "scripts": {
    "unit": "tape bin/test/**/*Test.js | tspec",
    "test": "npm run lint && npm run compile && npm run unit",
    "lint": "tslint --project tsconfig.json --config tslint.yaml",
    "coveralls": "npm run compile && npm run coverage -s && coveralls < coverage/lcov.info",
    "coverage": "npm run compile && nyc npm run unit",
    "compile": "tsc -d",
    "prepublishOnly": "npm run compile -s"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/revane.git"
  },
  "dependencies": {
    "config": "^3.0.1",
    "revane-fastify": "^2.0.2",
    "revane-ioc": "^1.0.2",
    "tslib": "^1.9.3"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/node": "^10.12.21",
    "coveralls": "^3.0.1",
    "nyc": "^13.2.0",
    "request": "^2.88.0",
    "tap-spec": "^5.0.0",
    "tape": "^4.9.1",
    "tape-catch": "^1.0.6",
    "ts-node": "^8.0.3",
    "tslint": "^5.10.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^2.9.1"
  },
  "keywords": [
    "inversion of control",
    "ioc",
    "dependency injection"
  ],
  "types": "./bin/src/Revane.d.ts"
}
